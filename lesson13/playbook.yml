---
- name: Deploy WordPress with MySQL and Apache
  hosts: all
  become: yes
  vars:
    mysql_root_password: ChangeMe123!
    mysql_database: wordpress
    mysql_user: wordpress_user
    mysql_password: wordpress_password

  roles:
    - mysql_db
    - apache_web
